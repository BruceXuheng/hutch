# Hutch · 器舍

> 一个顺手就打开的个人工具箱——本地前端工具、联网 AI 助手、精选在线服务，统一入口。

---

## 1. 一句话定位

**Hutch·器舍** 是一个纯静态的个人工具箱站点：把分散在各处的工具（本地前端小工具、联网 AI 助手、外部在线服务链接）聚合到一个深色科技风的检索入口里，即搜即用。

---

## 2. 名字含义

- **Hutch**（英文）：储物柜、小屋——装趁手工具的柜子。
- **器舍**（中文）："器"=工具，"舍"=居所——工具的家。
- 中英两名一意双形，都指向"工具的居所"这个核心意象：不宏大、不花哨，是一个顺手就能打开、装满趁手工具的小柜子。

---

## 3. 愿景

把"找工具"这件事的成本压到最低。

日常里工具是分散的：JSON 格式化、正则测试、取色、二维码是本地小工具；润色、翻译、代码解读是 AI 工具；regex101、Excalidraw、remove.bg 是外部在线服务。它们散落在书签、历史记录、各自网址里，每次用都要"想一下在哪"。

Hutch·器舍 把它们收进一个入口，靠"关键词 + 正文中英文检索"一搜即得，把"找"的时间从分钟级压到秒级。**自用为主，顺便开源**，让同样有这个需求的人也能 fork 一份自用。

---

## 4. 受众

| 角色 | 说明 |
|---|---|
| 主用户 | 自己（开发者），日常需要各类小工具的快速调用 |
| 次用户 | 任何从 GitHub 看到 repo、fork 或直接访问站点的人 |
| 录入者 | 仅自己一人——负责收录/维护工具列表 |

> 不做面向非技术用户的录入表单、不做多用户体系。录入 = 新建一个 Markdown 文件 push。

---

## 5. 核心价值

1. **统一入口**：三类工具（本地前端 / AI / 在线服务）一个地方找。
2. **即搜即用**：关键词 + 正文中英文 + 拼音首字母检索，秒级命中。
3. **顺手就开**：纯静态托管在 GitHub Pages，push 即发布，无需部署/后端/登录。
4. **录入自然**：每个工具一个 Markdown 文档，front matter 即元数据，正文即内容，拼音自动生成。
5. **克制好看**：深色科技风偏 Google（Material 3 Dark），不堆霓虹光晕，靠留白与层次。

---

## 6. 技术概览（一句话）

Markdown 驱动 + GitHub Actions 构建索引 + 纯前端检索。每个工具 = `tools/<id>.md`，push 后 Actions 自动扫描生成 `index.json`，前端加载它完成检索与渲染。

> 完整技术设计见 `TECH_DESIGN.md`，样式 demo 见 `design-demo.html`，产品需求见 `PRD.md`，设计方案见 `DESIGN.md`。

---

## 7. 项目边界（不做什么）

- 不做后端、不做数据库、不做用户登录。
- 不做运行时增删工具（工具列表静态写死在 md 里，发布即固定）。
- 不做错别字/模糊容错检索（按需简单化，`indexOf` 包含匹配）。
- 不做跨设备云端同步（最近使用等后续用 localStorage，仅本机）。
- 不做面向非技术用户的录入表单。

---

## 8. 当前状态

- [x] 方案选型：方案 A（Markdown + Actions 构建索引）
- [x] 技术设计：`TECH_DESIGN.md`
- [x] 样式 demo：`design-demo.html`（Material 3 Dark）
- [x] 项目定义：本文档
- [ ] PRD：`PRD.md`
- [ ] 设计方案：`DESIGN.md`
- [ ] 开发规划
- [ ] 实现落地

---

## 9. 仓库信息（待建）

- 仓库名：`hutch`（或个人偏好）
- Pages 地址：`https://<用户名>.github.io/hutch/`
- 主分支：`main`，Pages 源 = `main` / root
- 构建自动化：`.github/workflows/build.yml`（push 触发，生成 `index.json` 提交回仓库）
